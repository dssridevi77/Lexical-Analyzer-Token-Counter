#include <stdio.h>

int main()
{
    int a = 10;
    int b = 20;
    int sum = a + b;

    // Calculate sum
    printf("Sum = %d", sum);

    if (sum > 20)
    {
        printf("Sum is greater than 20");
    }

    return 0;
}